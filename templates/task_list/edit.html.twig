{% extends 'base.html.twig' %}

{% block breadcrumbs %}
    <nav aria-label="breadcrumb">
        <ul>
            <li><a href="{{ path('app_task_list_index') }}">Home</a></li>
            <li><a href="{{ path('app_task_list_show', {id: task_list.id}) }}">{{ task_list }}</a></li>
            <li><a href="{{ path('app_task_list_edit', {id: task_list.id}) }}">Edit</a></li>
        </ul>
    </nav>
{% endblock %}

{% block title %}Edit {{ task_list }}{% endblock %}
{% block main_title %}{% endblock %}
{% block body %}
    {{ component('task_list_type', { 'taskList': task_list.id ? task_list : null, 'form': form }) }}
{% endblock %}
